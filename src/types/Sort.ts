export enum SortMethod {
    bodyPart = 'bodyPart',
    equipment = 'equipment',
    id = 'id',
    name = 'name',
    target = 'target',
    difficulty = 'difficulty',
    category = 'category',
}

export enum SortOrder {
    ascending = 'ascending',
    descending = 'descending',
}
