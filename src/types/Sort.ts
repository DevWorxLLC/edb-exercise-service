export enum SortMethod {
    bodyPart = 'bodyPart',
    equipment = 'equipment',
    id = 'id',
    name = 'name',
    target = 'target',
}

export enum SortOrder {
    ascending = 'ascending',
    descending = 'descending',
}
